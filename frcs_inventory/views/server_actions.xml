<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Apply FRCS VAT 12.5% (Sales) to selected products -->
  <record id="action_apply_vat_125_sales" model="ir.actions.server">
    <field name="name">Apply FRCS VAT 12.5% (Sales)</field>
    <field name="model_id" ref="product.model_product_template"/>
    <field name="binding_model_id" ref="product.model_product_template"/>
    <field name="binding_type">action</field>
    <field name="state">code</field>
    <field name="code"><![CDATA[
tax = env.ref('l10n_fj_minicoa.tax_frcs_vat_125_sales', raise_if_not_found=False)
if not tax:
    tax = env['account.tax'].search([
        ('type_tax_use', '=', 'sale'),
        ('amount_type', '=', 'percent'),
        ('amount', '=', 12.5),
        ('active', '=', True),
    ], limit=1)
if not tax:
    raise ValueError('FRCS VAT 12.5% (Sales) not found')
for rec in records:
    if rec.exists():
        rec.write({'taxes_id': [(6, 0, [tax.id])]})
    ]]></field>
  </record>

  <!-- Apply FRCS VAT 15% (Sales) to selected products -->
  <record id="action_apply_vat_15_sales" model="ir.actions.server">
    <field name="name">Apply FRCS VAT 15% (Sales)</field>
    <field name="model_id" ref="product.model_product_template"/>
    <field name="binding_model_id" ref="product.model_product_template"/>
    <field name="binding_type">action</field>
    <field name="state">code</field>
    <field name="code"><![CDATA[
tax = env.ref('l10n_fj_minicoa.tax_frcs_vat_15_sales', raise_if_not_found=False)
if not tax:
    tax = env['account.tax'].search([
        ('type_tax_use', '=', 'sale'),
        ('amount_type', '=', 'percent'),
        ('amount', '=', 15.0),
        ('active', '=', True),
    ], limit=1)
if not tax:
    raise ValueError('FRCS VAT 15% (Sales) not found')
for rec in records:
    if rec.exists():
        rec.write({'taxes_id': [(6, 0, [tax.id])]})
    ]]></field>
  </record>

  <!-- Apply FRCS VAT 0% (Sales) to selected products -->
  <record id="action_apply_vat_0_sales" model="ir.actions.server">
    <field name="name">Apply FRCS VAT 0% (Sales)</field>
    <field name="model_id" ref="product.model_product_template"/>
    <field name="binding_model_id" ref="product.model_product_template"/>
    <field name="binding_type">action</field>
    <field name="state">code</field>
    <field name="code"><![CDATA[
tax = env.ref('l10n_fj_minicoa.tax_frcs_vat_0_sales', raise_if_not_found=False)
if not tax:
    tax = env['account.tax'].search([
        ('type_tax_use', '=', 'sale'),
        ('amount_type', '=', 'percent'),
        ('amount', '=', 0.0),
        ('active', '=', True),
    ], limit=1)
if not tax:
    raise ValueError('FRCS VAT 0% (Sales) not found')
for rec in records:
    if rec.exists():
        rec.write({'taxes_id': [(6, 0, [tax.id])]})
    ]]></field>
  </record>

  <!-- Remove Customer Taxes from selected products -->
  <record id="action_remove_customer_taxes" model="ir.actions.server">
    <field name="name">Remove Customer Taxes</field>
    <field name="model_id" ref="product.model_product_template"/>
    <field name="binding_model_id" ref="product.model_product_template"/>
    <field name="binding_type">action</field>
    <field name="state">code</field>
    <field name="code"><![CDATA[
for rec in records:
    if rec.exists():
        rec.write({'taxes_id': [(5, 0, 0)]})
    ]]></field>
  </record>
</odoo>

